# CUDA N-Body Simulation - Python Dependencies
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.19.0

# Visualization
matplotlib>=3.3.0

# GPU acceleration with CUDA support
numba>=0.53.0

# CUDA toolkit bindings (for GPU support)
# Note: Requires NVIDIA GPU and CUDA toolkit installed on system
# cudatoolkit>=10.2

# Note: FFmpeg is required for video export but must be installed separately:
# Ubuntu/Debian: sudo apt install ffmpeg
# RHEL/CentOS: sudo yum install ffmpeg
# Windows: Download from https://ffmpeg.org/
